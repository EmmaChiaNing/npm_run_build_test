import KeepAliveView from '../../views/KeepAliveView.vue'

const keepAlive = [
    {
        path: '/keepAlive',
        name: 'keepAlive',
        component: KeepAliveView,
    }
]

export default keepAlive;