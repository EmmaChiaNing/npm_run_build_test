import LazyLoadComponents from '../../views/LazyLoadComponents.vue'

const lazyLoadComponents = [
    {
        path: '/lazyLoadComponent',
        name: 'lazyLoadComponent',
        component: LazyLoadComponents
    }
]

export default lazyLoadComponents;